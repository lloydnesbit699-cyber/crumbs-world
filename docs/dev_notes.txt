editor dev notes for fixes upgrades

sprites only get 2 frames for animations, definatly need to expand on on that aspect. also sprites are randomly generated in a set pattern within each map that is generated... theres no popup pallets for env objects inventory objects  tiles textures ect... no paint function at all so no variations of paint brush no eyedropper . wasd controls the map in play mode but the sprite has no movement, sprite still the same size as tile, should be 1/2 maybe 1/3 size of tiles... biomes gen very well but they dont have defining character differences(each selector can generate every biome) no hide side panel... umm should be able to upload and auto catagorize imgs into seperate sections... ie tiles texture world object inventory objects sprite creatures ect and each asset type gets a name generator so that once uploaded it can be saved needs cached saving for undo redo eyedropper is a much paint bruch sizes and shapes a drawing function that can produce different shapes and styles to be saved as tiles for continuous reusability... loving the animator working, needs expansion.... 