# Export Packs

Export packs define target-specific packaging profiles.

- `tiled.pack.json` for Tiled-style JSON flows
- `godot.pack.json` for Godot-import workflows
- `unity.pack.json` for Unity-import workflows

Each pack includes:
- target id
- supported map/bundle version ranges
- expected output files
- integration notes
